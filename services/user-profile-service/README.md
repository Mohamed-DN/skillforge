# User Profile Service

Manages user profiles, competency vectors, and career goals.

## Running
```bash
cd services/user-profile-service
pip install -r requirements.txt
uvicorn src.main:app --reload --port 8003
```
