# Notification Service

Sends push notifications, emails, and in-app alerts based on learning milestones.

## Running
```bash
cd services/notification-service
pip install -r requirements.txt
uvicorn src.main:app --reload --port 8004
```
