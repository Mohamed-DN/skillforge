# CV Analyzer

Processes uploaded CVs (PDF/DOCX), extracts skills, and builds the initial competency vector.

## Running
```bash
cd services/cv-analyzer
pip install -r requirements.txt
python -m src.main
```
